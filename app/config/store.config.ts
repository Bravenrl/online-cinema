export enum Reducer {
  User = 'USER',
}

export enum Slice {
  AppUser = 'user',
}

export enum AsyncThunk {
  CheckAuthStatus = 'user/checkAuthStatus',
  Login = 'user/login',
  Logout = 'user/logout',
  Registration = 'user/registration',
}
