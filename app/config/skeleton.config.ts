import { MAX_GENRE_ITEMS } from './menu.config';

export const GENRE_SKELETON_COUNT = MAX_GENRE_ITEMS + 1;

export const POPULAR_COUNT = 3;

export const FAVORITES_COUNT = 3;

export enum SkeletonConfig {
  BaseColor = '#1F2125',
  HighlightColor = '#292A2E',
}
